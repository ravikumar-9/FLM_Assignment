# Companies Directory – Frontend Assignment

This is a responsive React application built for the Frontlines Media frontend assignment.  
The app displays a list of companies, supports searching and filtering, and presents the data in a clean table layout.

---

## 🚀 Features

### ✅ Core Functionality

- Fetches company data from a **static `companies.json` file**.
- Displays companies in a **responsive table**.
- Supports:
  - Search by company name
  - Filter by **Industry**
  - Filter by **Location**
- Clean UI built with **Tailwind CSS**.
- **Reusable Search + Filter component**.
- Smooth loading state and no-results state.

### ✅ Tech Stack

- **React** (Vite)
- **TypeScript**
- **Tailwind CSS**
- **Static JSON** for mock API
